// PackageManager.cpp
#include "PackageManager.h"

void PackageManager::storePackage(int packageId, int lockerId) {
    // 存储包裹信息
}

void PackageManager::updatePackageStatus(int packageId, const QString& status) {
    // 更新包裹状态
}